[pytest]
testpaths = tests
python_files = test_*.py
# Quiet by default: the modules under test print progress diagnostics, and a
# wall of build output makes a failing assertion hard to find.
addopts = -q --tb=short
filterwarnings =
    ignore::DeprecationWarning
